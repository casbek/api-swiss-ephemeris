# Source files are always stored with LF in the repository.
# The deployment target is Linux, where CRLF breaks builds and shell scripts.
* text=auto eol=lf

# Ephemeris data files are binary and must never be converted.
*.se1 binary
